<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tyga.fintech">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoActionBar">
        <activity android:name=".ProfileMerchantActivity"></activity>
        <activity android:name=".RegisterMerchantActivity" />
        <activity android:name=".RegisterChooseActivity" />
        <activity android:name=".QRScannerActivity" />
        <activity android:name=".HistoryMerchantActivity" />
        <activity android:name=".HistoryNasabahActivity" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".TopUpActivity" />
        <activity android:name=".MerchantActivity" />
        <activity android:name=".QRGeneratorActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailPromoActivity" />
        <activity android:name=".PromoActivity" />
        <activity android:name=".DetailMerchantActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>